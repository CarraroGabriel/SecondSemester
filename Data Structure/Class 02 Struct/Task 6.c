#include <stdio.h>
#define MAX 5

struct livro {
	char titulo;
	char autor;
	int ano;
	float preco;
};

int main () {
	
	struct livro book;
	
	for ( i = 0; i < MAX; i++) {
	scanf("%s", book[i].titulo);
	scanf("%s", book[i].autor);
	scanf("%i", &book[i].ano);
	scanf("%f", &book[i].preco)
}
	
	
	return 0;
}
